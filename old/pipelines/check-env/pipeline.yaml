name: check-env
description: 检查 team-pivot 所需的外部工具是否已安装
triggers:
  - "check environment"
  - "检查环境"
  - "环境检查"

input: {}

steps:
  - name: check
    type: code
    command: "python3 steps/check.py"

output: check
