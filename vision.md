# Pivot Vision

> **给 AI 看的项目愿景文档。** 新 session 进入项目先读 `memo.md`（现状），再读这份（目标），就能判断当前任务属于哪个阶段、是否符合方向。
>
> 这份文档讲"要做什么、为什么、还差什么"。具体怎么实现的看 `memo.md`。

---

## 一、Pivot 是什么

Pivot 是一个 **AI 驱动的全生命周期团队管理系统**。基于 **MD + Git** 存储所有团队工作的过程文档，让 AI 接管"组织运转"——人提供智慧（创意 / 判断 / 决策 / 专业执行），AI 推动文档化、流程衔接、信息流转、进度追踪、知识沉淀。

**终极形态**：一家中小公司用 Pivot 替代 Jira / 飞书项目 / 钉钉智能工作台等工具，实现 GTD 全流程的 AI 原生执行。

不限于软件研发——运营、市场、创意、行政等任何项目化工作都适用。

---

## 二、核心理念（5 条）

1. **AI 是组织运转的引擎，不是工具**——传统公司的执行架构是用人弥补"信息不对称 / 协调成本 / 过程文档化"三个约束。AI 同时瓦解这三者。
2. **MD + Git 是天然载体**——AI 可读、文本可结构化、版本可追溯、零基础设施。
3. **过程文档化第一次零成本**——AI 既是组织活动的执行者也是记录者，文档化是业务运转的副产品而非额外负担。这构成公司的"数字化分身"，是 AI 真正能"操作组织"的前提。
4. **AI 推动，人决策**——关键节点设人工确认门（需求确认 / 任务拆分 / 任务分配等）。AI 给建议，人拍板。
5. **always review > weekly review**——传统 GTD 难坚持的"每周回顾"由 AI 持续巡视自动实现。

---

## 三、系统架构

### 两种 AI 的分工

**组织引擎 AI（系统级，一个）**
- 全局视角，持续巡视所有 thread / project / task
- 编写和维护管理文档（需求、项目、任务、验收、知识）
- 主动提醒、协调资源、预警风险、生成报告
- 调度每个员工的下一步工作
- 项目完成后提炼知识、发现模式、改进工作方法
- **不做具体业务执行，但驱动一切组织层面的运转**

**员工搭档 AI（个人级，每人一个）**
- 帮员工阅读和理解相关文档
- 协助执行具体任务（编码 / 设计 / 写文案 / 写方案）
- 任务完成后总结成果回写系统

### Git 仓库 = 共享大脑

不只是文件存储，而是所有 AI 的**信息中枢**——组织引擎 AI 和所有员工搭档 AI 读写同一组 MD/YAML 文件，通过共享知识体感知组织状态，无需任何直接通信机制。

---

## 四、全生命周期（GTD 五步 + Review/Evolve）

```
1. Capture（发起讨论）    想法 → 讨论                       任何想法先发起讨论作为收集箱
2. Clarify（达成共识）    讨论 → 结论 → 需求文档            [人工确认门 1] 团队厘清，AI 生成 RESULT
3. Organize（规划落地）   需求 → 项目 → 任务拆分 → 分配     [人工确认门 2、3] AI 拆，人决
4. Reflect（持续监控）    AI 全局巡视                       替代每周回顾，主动推动状态流转
5. Engage（专注执行）     员工 + 搭档 AI 推进任务            人只做这一步
─────────── 超越 GTD ───────────
6. Review（复盘沉淀）     验收 → 复盘 → 知识沉淀            文档化让复盘素材天然完整
7. Evolve（持续进化）     发现模式 → 改进工作方法           从历史项目优化系统本身
```

### 多入口

不必从头走完整流程：

- 模糊想法 → 从 1. 发起讨论
- 想清楚要做什么 → 跳到 3. 规划落地
- 独立小事 → 直接 5. 创建任务执行
- 任务执行中需要讨论 → 触发子讨论（回到 1），结论回挂到原任务

---

## 五、数据模型

### 顶层目录

```
discussions/    ← 发起讨论 + 达成共识（已实现）
projects/       ← 规划落地 + 专注执行 + 复盘沉淀（待实现）
knowledge/      ← 持续进化（待实现）
index/          ← 所有生命周期链路的索引 YAML
```

每条生命周期链路有一个 INDEX 文件（如 `index/auth-redesign-discuss.index.yaml`），是 AI 理解项目全貌的入口——读 INDEX 不需要扫描所有原始文件。一个文件可以属于多条链路。

### discuss 状态机（5 状态，已实现）

| 状态 | 含义 |
|---|---|
| `open` | 讨论进行中 |
| `concluded` | 已达成结论，RESULT 已生成 |
| `produced` | 结论已转化为项目 |
| `closed` | 关闭，无需行动 |
| `pending` | 暂时搁置 |

**流转规则：**
- 自动：`open → concluded`（生成 RESULT）/ `concluded → produced`（创建项目）
- 人工：`open → closed/pending`、`pending → open`
- reopen 动作（需说明原因）：`concluded/closed → open`，状态回 `open` 但 timeline 记录回退原因
- `produced` 不可回退，问题在项目层面处理

状态**只存 INDEX**（业务文件不存）——单一事实源。

### INDEX 文件（YAML）

包含两部分：
- **业务对象清单**：状态、文件清单、关系（`from` / `refer` / `blocked_by`）、摘要
- **timeline**：演进记录，含 mention 和 comments

**文件关系类型：**

| 关系 | 含义 | 示例 |
|---|---|---|
| `from` | 衍生自（同一链路纵向） | RESULT 从讨论帖总结而来 |
| `refer` | 引用（跨链路横向） | 讨论帖引用其他项目文档 |
| `blocked_by` | 依赖（待实现） | 任务 A 等待任务 B 完成 |

### 业务文件 frontmatter

```yaml
---
type: proposal | reply | result | requirement | task | ...
author: <pinyin>
created: <ISO date>
auto-summary: <AI 生成的摘要>
index_state: indexed | un-indexed   # 两阶段原子写标记
---
```

### 文件命名

- 讨论帖：`<NNN>_<author>_<type>.md` 顺序编号
- 未来其他类型文件：附 6 位十六进制随机短哈希作为永久 ID（如 `任务_b7e1.md`），文件移动 / 改名后引用方仍可通过 `glob **/*_b7e1.md` 定位

### 原子性保证

`un-indexed` 标记 + 三步操作（写文件 → 更新 INDEX → 翻标志）+ 启动恢复巡视。详见 `memo.md §6`。

---

## 六、当前实现状态

> 详细实现见 `memo.md`。这里只盘大块，方便对照路线图。

### ✅ 已完成（discuss 模块基本闭环）

- 飞书 OAuth 登录 + 首登 pinyin 收集
- 5 状态机 + 转移（含 reopen 原因记录）
- INDEX 文件读写 + `from` / `refer` 关系
- 两阶段原子写 + 启动恢复
- @mention + 飞书 DM/群卡片通知
- 草稿 autosave + 内联回复
- AI 助手（OpenRouter 流式），针对回复对象 + 引用文件生成回复草稿，对话持久化
- PAT（VS Code 等外部客户端）
- 管理员密码门（AI 配置 / 联系人同步）

### ⚠️ 部分完成

- **AI 摘要**：post `auto-summary` 字段已定义、INDEX `files[].summary` 字段已预留，AI 写入未实现
- **AI 助手定位**：当前 AIPane 只是"线程内回复助手"，远未达到"员工搭档 AI"的定位
- **异常恢复**：两阶段标记 + 启动巡视有，定时巡视未做

### ❌ 未实现（关键缺口）

| 缺口 | 对应 GTD 阶段 | 影响 |
|---|---|---|
| AI 自动生成 RESULT | Clarify 收口 | RESULT 当前需人工写，闭环断裂 |
| AI 摘要写入 | 全流程 | INDEX 摘要靠人，下游 AI 无法高效读取 |
| **monitor 模块**（巡视 / 提醒 / 推动） | Reflect | 缺这个 Pivot 就只是被动工具，不是"组织引擎" |
| **project 模块**（需求 / 项目文件） | Organize | GTD 第 3 步入口，全链路下游全部缺失 |
| **task 模块**（拆分 / 分配 / 进度 / 阻塞） | Organize + Engage | 团队真正干活的承载体 |
| 任务调度（"下一步该做什么"推送） | Engage | 释放员工认知负担的核心机制 |
| **knowledge 模块**（验收 / 复盘 / 经验） | Review / Evolve | 组织记忆和进化能力 |
| 子讨论回挂任务 | 多入口 | 任务卡住时的疏导机制 |
| 多入口支持 | 入口 | 当前只能从 discuss 进 |
| 三个人工确认门的工作流 | Clarify + Organize | 治理机制 |
| 全维度可视化报告 | Reflect | 员工 / 项目 / 组织 / 知识 / 时间维度 |

---

## 七、路线图（按优先级）

### Phase A：discuss 闭环自动化（1-2 周）

- **A1. AI 摘要**：post 写入时自动生成 `auto-summary`，INDEX `files[].summary` 同步写入
- **A2. AI 生成 RESULT**：基于讨论全部 post 自动起草 RESULT 文件，人工确认门 1 后保存

### Phase B：组织引擎 AI 第一次现身（2-3 周）

- **B1. monitor-scan**：定时巡视所有 INDEX，识别模式
  - 长期 open 未更新
  - mention 后未回复
  - concluded 未 promote 为项目
- **B2. 主动通知**：复用现有飞书卡片，AI 推送提醒
- **B3. 异常恢复定时巡视**：扫 un-indexed 文件自动修复

> **里程碑意义**：这一阶段团队第一次"感受到"AI 在主动推自己——心理冲击最大，最能验证 AI 原生方向。

### Phase C：project 模块（3-4 周）

- C1. 从 RESULT → 需求文档：AI 起草 + 人工确认门 2
- C2. `projects/` 目录结构 + project INDEX
- C3. `discuss → project` 的 `produced` 状态自动联动

### Phase D：task 模块（3-4 周）

- D1. 任务拆分：AI 从需求文档拆 + 人工确认门 3
- D2. 任务分配建议：AI 推荐 owner + 人工确认
- D3. 任务进度记录、阻塞标记、依赖（`blocked_by`）
- D4. 任务的 subdiscussion 回挂机制

### Phase E：员工搭档 AI 升级（持续）

- E1. "今天该做什么"个人推送：基于全局 task 状态、截止日、依赖关系
- E2. 协助任务执行：自动带入相关讨论 / 需求 / 历史项目上下文
- E3. 任务完成自动总结写回

### Phase F：knowledge / Review / Evolve（远期）

- F1. 验收 checklist + 自动验收报告
- F2. 项目复盘文档自动生成
- F3. 跨项目模式发现（工期偏差 / 阻塞高发阶段 / 共识达成模式 / 验收遗漏点）
- F4. 组织知识库 + 新人 onboarding 检索

### Phase G：可视化报告（穿插）

五维度报告——员工 / 项目 / 组织 / 知识 / 时间。**不是新建功能**，是从 INDEX + 文件流自然汇总。

---

## 八、设计原则（与 `memo.md §11` 互补）

1. **AI 是唯一写入层**——所有写操作通过单一 Git 服务账号串行执行，无并发冲突
2. **单文件 = 完整信息**——MD 文件内聚合元数据 + 正文 + 进展，不依赖外部索引也能读懂单个文件
3. **INDEX 是单一事实源**——状态、关系、摘要只存 INDEX，业务文件不重复
4. **不引入数据库索引**——查询靠 AI 扫描，中小团队规模性能不是瓶颈
5. **AI 推动，人决策**——关键节点必须有人工确认门，AI 不能自己关闭循环
6. **面向中小团队**——聚焦核心链路，不过早考虑大规模
7. **不限于软件研发**——任何项目化工作都应能承载

---

## 九、与 Jira 的本质区别（一表概括）

| 维度 | Jira | Pivot |
|---|---|---|
| 数据格式 | 数据库字段 | MD + YAML |
| 操作方式 | 人填表 | 人和 AI 对话 |
| 内容深度 | 标题 + 几行 | 完整结构化文档 |
| AI 可读性 | 需 API 适配 | 天然可读 |
| 阶段衔接 | 人工搬运 | AI 自动起草 |
| 流程推动 | 被动等人操作 | AI 主动推，人确认 |
| 复盘沉淀 | 额外工作 | 文档流自然产物 |
| 版本控制 | 内置不透明 | Git，完全透明 |

---

## 十、新 AI 起手式

1. **先读 `memo.md`**——掌握当前实现细节、目录结构、约束、踩过的坑
2. **再读这份 `vision.md`**——明白终极目标、剩余缺口、下一步优先级
3. **改代码前**：对照 §6（当前状态）和 §7（路线图），确认你要做的事属于哪个 Phase
4. **不要违反 §8 设计原则**——比如引数据库索引、跳过人工确认门、并发写 Git
5. **方向不确定时问用户**——这是铁律，`memo.md §11` 也写了

---

## 附：文档来源

本文档由以下三份合并精炼而来（已删除过时内容如 EC APP / Pipeline 框架 / CLI 命令 / 飞书 WebView 内嵌等——Pivot 现已是独立 Web 应用）：

- `teamDocs/discussions/discussion/ai驱动的gtd全生命周期项目管理系统/001_*_proposal.md` — 产品愿景和 GTD 映射
- `teamDocs/discussions/discussion/ai驱动的gtd全生命周期项目管理系统/004_*_reply.md` — discuss 模块详细设计（数据结构部分仍然有效）
- `team_work_thinking/AI native company/AI原生的公司方法论.md` — 范式转换和四个支柱（精简到核心理念 5 条）
