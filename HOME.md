# 欢迎使用 Pivot

## 它是做什么的
Pivot 是一个以 Markdown 和 Git 为中心的团队讨论工作台。每个讨论主题都可以沉淀想法、行动、验证、洞察和结果，让证据、决策与后续推进留在同一条时间线里。

## 快速开始
1. 点击顶部 `新讨论`，创建一个新的讨论主题。
2. 在讨论里持续追加 `think / act / verify / insight / result` 文件，补齐背景、执行、验证和结论。
3. 通过状态流转标记讨论进度，并在形成结果后用 `result` 收口。

## 推荐工作流
1. 先把要解决的问题写成一个讨论主题，而不是散落在聊天记录里。
2. 每次推进都补充可追溯的文件：想法、行动、验证、引用和评论都沉淀到时间线。
3. 需要 AI 协助时，直接在讨论内发起对话，让 AI 读取当前上下文并生成草稿。
4. 重要讨论用收藏、未读、状态和负责人保持可见，定期用 Git 同步工作区。

## 当前可用能力
- 讨论列表、详情、收藏、未读、负责人和六态状态流转
- 讨论时间线与 `think / act / verify / insight / result` 文件模型
- 草稿自动保存、恢复、发布，以及从 AI 回复生成草稿
- 讨论内评论、提及、飞书通知和实时刷新
- AI 工具可检索讨论、读取索引和文件，并通过 MCP 接入外部 AI 客户端
- legacy thread 到新索引格式的迁移脚本

## 使用建议
- Category 建议按团队或主题长期固定，讨论标题写清楚要解决的问题。
- `result` 用来正式收口讨论；未完成的推进优先写成 `act / verify / insight`。
- 如果旧 thread 还需要继续维护，建议先迁移到新的时间线工作流。
