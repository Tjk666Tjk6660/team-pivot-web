name: discuss-status
description: "变更讨论状态：close / pending / reopen"
triggers: ["关闭讨论", "搁置", "重新打开", "close thread", "reopen"]
input:
  category: string
  thread: string
  action: string
  reason: string
steps:
  - name: change_status
    type: code
    command: "python3 steps/change_status.py"
output: change_status
