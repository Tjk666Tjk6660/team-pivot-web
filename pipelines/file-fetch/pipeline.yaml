name: file-fetch
description: "读取一个或多个文件，自动附带关联的 INDEX 文件"
triggers: ["读文件", "fetch file"]
input:
  paths: string
steps:
  - name: fetch
    type: code
    command: "python3 steps/fetch.py"
output: fetch
