name: _constructor
description: "Pre-pipeline checks: config validation, data_space sync"

steps:
  - name: check_config
    type: code
    command: "python3 steps/check_config.py"

  - name: git_sync
    type: code
    command: "python3 steps/git_sync.py"

output: check_config
